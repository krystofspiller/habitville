package pages

import (
	"github.com/Laegas/habitville/templates/components"
	"github.com/Laegas/habitville/models/types"
)

templ CreateHabit() {
	@components.Form("/api/habits") {
		@components.Input(types.Text, "Habit name", "name")
		@components.Input(types.Number, "Revenue", "revenue")
	}
}
